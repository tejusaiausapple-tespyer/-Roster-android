*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
app/release/
app/debug/
# google-services.json is committed deliberately — it's Firebase client
# config, not a secret (Google ships it inside every compiled APK), and CI
# needs it present to run processDebugGoogleServices/lint/assemble.
*.jks
*.keystore
keystore.properties
